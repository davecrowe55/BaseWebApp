<html lang="en">
<head>
    <% include ../helpers/head %>
</head>

<body>
  <script src="https://www.gstatic.com/firebasejs/7.20.0/firebase-app.js"></script>
  <!-- the classes 'page-header' and 'btn btn-default' create bootstrap components.  -->
  <!-- Check out more components here: http://getbootstrap.com/components/ -->
  <script> var firebaseConfig = {
    apiKey: "<%= process.env.API_KEY %>",
    authDomain: "base-web-app-852d7.firebaseapp.com",
    databaseURL: "https://base-web-app-852d7-default-rtdb.firebaseio.com",
    projectId: "base-web-app-852d7",
    storageBucket: "base-web-app-852d7.appspot.com",
    messagingSenderId: "164456799690",
    appId: "<%= process.env.FIREBASE_API_ID %>",
    measurementId: "G-YFG35JEVHT"
  };

// we skip the "already exists" message which is
// not an actual error when we're hot-reloading
if (!/already exists/.test(err.message)) {
console.error('Firebase initialization error raised', err.stack)

const firebaseApp= firebase;
  // Initialize Firebase
  if (!firebase.apps.length) {
    firebase.initializeApp({});
}
  firebase.initializeApp(firebaseConfig);
  firebase.analytics();
</script>
  <div class="page-header">
    <h1>Add a post</h1>
  </div>

  <button class="btn btn-default" type="button" id="login-button" onclick="toggleSignIn()">Log in</button>
  <button onclick="hello();">Hello</button>
  <form action="/action_page.php">
    Post Title:<br>
    <input type="text" id="post-title"><br>
    Post Body:<br>
    <input type="text" id="post-body"><br><br>
    <button class="btn btn-default" type="button" onclick="handleMessageFormSubmit()">
      Save it
    </button>
  </form>

  <div id="stream"></div>
  <h1>Welcome to My Awesome App</h1>
<div id="firebaseui-auth-container"></div>
<div id="loader">Loading...</div>

</body>

</html>

</html>

